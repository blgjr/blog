<h1>Новый пост</h1>

<%= form_for :post, url: posts_path do |f| %>
	<p>
		<%= f.label :title, 'Заголовок' %><br>
		<%= f.text_field :title %>
	</p>

	<p>
		<%= f.label :text, 'Текст' %><br>
		<%= f.text_area :text %>
	</p>

	<p>
		<%= f.submit :'Запостить' %>
	</p>
<% end %>